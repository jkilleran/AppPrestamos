Future<void> openDocumentWithAuth({
  required Uri uri,
  required String token,
  required String filenameFallback,
}) {
  throw UnsupportedError('Plataforma no soportada para abrir documentos.');
}
